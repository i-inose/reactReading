// ============================================================
// UserList.tsx ― オンラインユーザー一覧
//
// 【このファイルで学べること】
// - React.memo による不要な再レンダリングの防止
// - Props の型定義とシンプルなリスト表示
//
// 【04-realtime-chat との対応】
// chat にも同名の UserList.tsx があり、ほぼ同じ構造。
// ここでは React.memo を使って、props が変わらない限り
// 再レンダリングしないように最適化する。
// ============================================================

import React from "react";

// --------------------------------------------------
// Props の型定義
// --------------------------------------------------
interface UserListProps {
  users: string[];         // オンラインユーザー名の配列
  currentUser: string;     // 自分のユーザー名
}

// --------------------------------------------------
// 【TODO(Q8)】React.memo でコンポーネントをメモ化する
//
// ヒント: React.memo は高階コンポーネント（HOC）で、
// props が変わらない場合に再レンダリングをスキップする。
// 投票アプリでは votes が頻繁に更新されるため、
// ユーザー一覧は props が変わらない限り再描画しないようにしたい。
//
// 実装すべきこと:
// 1. React.memo でコンポーネント関数を囲む
// 2. コンポーネント内でユーザー一覧を表示する:
//    a. "参加者 (N人)" のヘッダー
//    b. users.map でユーザー名のリストを表示
//    c. 自分のユーザー名には「（自分）」ラベルを付ける
//
// 参考:
//   export const ComponentName = React.memo(function ComponentName(props: Props) {
//     return ( ... );
//   });
// --------------------------------------------------
export const UserList = undefined as any;
// ↑ ここを React.memo で囲んだコンポーネントに置き換える
//
// 仮実装（動作確認用）: props を受け取るだけの空コンポーネント
// 正しい実装に置き換えてください
void React; // React.memo 用に import しているが、TODO が未実装なので lint エラー回避
void ((_props: UserListProps) => null); // 型参照を維持する
