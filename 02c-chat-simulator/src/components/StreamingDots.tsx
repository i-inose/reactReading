// ============================================================
// TODO(Q9): ストリーミング中のドットアニメーションコンポーネントを実装してください
//
// ヒント:
// - 3つの <span> 要素でドット（.）を表示します
// - 各ドットに className="dot" を付与します
// - CSS アニメーション（App.css で定義済み）で順番に点滅します
// - animation-delay を inline style で指定して順番にアニメーションさせます
//
// JSX の構造:
//   <span className="streaming-dots">
//     <span className="dot" style={{ animationDelay: "0s" }}>.</span>
//     <span className="dot" style={{ animationDelay: "0.2s" }}>.</span>
//     <span className="dot" style={{ animationDelay: "0.4s" }}>.</span>
//   </span>
//
// 参考: React で inline style を書く方法
//   style={{ プロパティ: "値" }} ← オブジェクトを渡す
// ============================================================
export function StreamingDots() {
  return undefined as any;
}
